# A host board contributes its sources through PYRO_HOST_HAL_SOURCES in
# board.cmake, not through a library target, so there is nothing to add
# here. The file exists because add_subdirectory() expects it.
